import React from 'react';
import ReactDOM from 'react-dom/client';
import CreditCalculator from './CreditCalculator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CreditCalculator />
  </React.StrictMode>
);
